{
  // Press F5 to launch the Extension Development Host with Bacchus VINE loaded.
  // The "watch:vscode" preLaunchTask builds both dist/extension.js and
  // dist/server.js via esbuild in watch mode, so edits rebuild automatically.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/packages/vscode",
        "${workspaceFolder}/packages/vscode/test-workspace"
      ],
      "outFiles": ["${workspaceFolder}/packages/vscode/dist/**/*.js"],
      "sourceMaps": true,
      "preLaunchTask": "watch:vscode"
    }
  ]
}
